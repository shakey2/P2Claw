{
  "id": "com.p2claw.demo-high-risk",
  "name": "Demo High Risk",
  "version": "0.1.0",
  "description": "First-party demo module that declares high-risk shell.execute. Use it to verify real Core-mediated subprocess execution with TOTP + audit.",
  "runtime": "inprocess",
  "firstParty": true,
  "entry": "index.js",
  "permissions": ["shell.execute", "log.info"],
  "tools": [
    {
      "name": "demo_shell",
      "description": "Runs a shell command through the broker's shell.execute path with TOTP approval and audit logging.",
      "parameters": {
        "type": "object",
        "properties": {
          "cmd": {
            "type": "string",
            "description": "The command name to execute."
          },
          "args": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Arguments to pass to the demo command."
          }
        },
        "required": ["cmd"]
      },
      "requires": ["shell.execute", "log.info"]
    }
  ]
}
